import 'package:flutter/material.dart';

class LabSchedulerPage extends StatelessWidget {
  const LabSchedulerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lab Scheduler')),
      body: const Center(child: Text('Lab Scheduler Page - Coming in Phase 1.2')),
    );
  }
}
