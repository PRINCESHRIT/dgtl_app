import 'package:flutter/material.dart';

class VitalsTrackingPage extends StatelessWidget {
  const VitalsTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Vitals Tracking')),
      body: const Center(child: Text('Vitals Tracking Page - Coming in Phase 1.2')),
    );
  }
}
