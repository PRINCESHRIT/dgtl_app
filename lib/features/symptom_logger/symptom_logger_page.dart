import 'package:flutter/material.dart';

class SymptomLoggerPage extends StatelessWidget {
  const SymptomLoggerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Symptom Logger')),
      body: const Center(child: Text('Symptom Logger Page - Coming in Phase 1.2')),
    );
  }
}
